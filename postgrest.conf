# PostgREST Configuration for Lexia CRM

db-uri = "postgres://localhost/lexia_crm"
db-schemas = "public"
db-anon-role = "mathisescriva"

server-host = "127.0.0.1"
server-port = 3001

# JWT secret (for authentication - optional for dev)
# jwt-secret = "your-secret-key"

# Enable CORS
# server-cors-allowed-origins = "http://localhost:3000"
